# Recordatorios remotos (backend campo-poudes-backend). Copiar a .env y completar.
# Sin estas vars, el push del feed es un no-op (la app funciona igual, solo sin email).
VITE_REMINDERS_API_URL=
VITE_REMINDERS_API_KEY=
